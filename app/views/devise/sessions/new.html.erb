<div class="login">
  <h2>Log in</h2>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= button_to "Sign in with #{OmniAuth::Utils.camelize(provider)}",
        omniauth_authorize_path(resource_name, provider),
        form_class: 'login__buttons',
        method: :post,
        data: { turbo: false } %>
    <br/>
  <% end %>
</div>
